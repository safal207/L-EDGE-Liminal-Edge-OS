# Edge Layer

SomaSeed gateway, который принимает внешний трафик, формирует EdgeEvent и передаёт его в слой хранения. В этой итерации
реализован заглушечный HTTP-сервис с эндпоинтами `/health` и `/echo`, а также интерфейс `EdgeEventSink` для проксирования
событий в `storage/`.
