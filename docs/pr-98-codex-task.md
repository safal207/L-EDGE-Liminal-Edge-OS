# PR-98 — Fix TypeScript errors and unblock L21 Flow Dashboard

## Background

В репозитории L-EDGE-Liminal-Edge-OS сейчас есть TypeScript-ошибки,
из-за которых `npm run check` падает и блокирует работу с L21 Flow Dashboard
и связанными примерами (`examples/L21FlowDashboardDemo`).

PR-97 описал общие цели и ограничения.  
PR-98 должен **реально исправить TypeScript-ошибки**, не ломая поведение
и не меняя внешние контракты.

---

## Goals

1. Сделать так, чтобы команда:

   ```bash
   npm run check
   ```

   проходила без ошибок TypeScript.

2. Сохранить существующее поведение рантайма:

   - не ломать API публичных функций,
   - не менять сигнатуры, от которых зависят другие части кода, если это не строго необходимо.

3. Разблокировать возможность дальше развивать L21 Flow Dashboard и другие примеры, которые полагаются на типобезопасный core/systemContext.

---

## Scope

В первую очередь нужно сфокусироваться на файлах, которые дают ошибки в `npm run check`. Ожидаемые проблемные зоны (пример, можно актуализировать по выводу компилятора):

- `src/core/systemContext.ts`
- `src/core/stateManager.ts`
- `src/core/types.ts`
- любые другие файлы `src/core/**`, где TS-ошибки не дают собрать проект
- при необходимости — типы/пропсы, используемые в `examples/L21FlowDashboardDemo` и базовых UI-компонентах (`src/components/ui/...`).

---

## Non-goals (что не делать)

- 🚫 Не переписывать архитектуру core или всего L21.
- 🚫 Не выносить крупные части логики в новые модули.
- 🚫 Не менять формат входных/выходных данных API, если это не требуется для устранения TS-ошибки.
- 🚫 Не добавлять новые зависимости в `package.json`, если без этого можно обойтись.
- 🚫 Не отключать строгие флаги TypeScript глобально (например, `strict: false`).

---

## Constraints / Style

1. Сначала типы, потом логика. По возможности исправлять ошибки через уточнение типов, а не через переписывание алгоритмов.
2. Prefer explicit types over `any`.
   - `any` допустим только как временный адаптер, если нет другой опции.
   - Если нужен «гибкий» тип, использовать:
     - `unknown` + type guards
     - `Record<string, unknown>`
     - или обобщения (`<T>`), если это вписывается в архитектуру.
3. Не ломать существующий контракт. Если исправление требует изменения типа параметра/возврата, убедись, что все места вызова обновлены согласованно и поведение не изменилось.
4. Минимальные правки логики. Избегать «умных» оптимизаций. Если приходится менять логику, добавить короткий комментарий `// TS fix: ...` с пояснением.

---

## Recommended approach

1. Запустить:

   ```bash
   npm install
   npm run check
   ```

   и зафиксировать список всех TS-ошибок.

2. Идти сверху вниз по ошибкам компилятора:

   - править типы и интерфейсы в `src/core/types.ts`, если они являются источником ошибок;
   - при необходимости уточнять типы в `systemContext`, `stateManager` и других core-файлах.

3. Для сложных структур состояния/контекста:

   - завести явный интерфейс/тип (например, `SystemContext`, `FlowState`, `LuckMetrics`),
   - использовать его вместо `any`/неявных структур.

4. Повторно запускать `npm run check`, пока все ошибки не будут устранены.

---

## Acceptance criteria

PR можно считать успешным, если выполняются ВСЕ условия:

1. `npm run check` проходит без ошибок TypeScript.
2. `npm test` (если есть настроенные тесты) проходит без падений.
3. Пример `L21FlowDashboardDemo` успешно собирается и запускается (если есть отдельная команда, указать её в описании PR).
4. В diff нет радикальных изменений логики; большинство изменений — это:
   - уточнение типов,
   - добавление/обновление интерфейсов,
   - корректное использование дженериков и `unknown` вместо `any`.
5. В описании PR кратко перечислены:
   - какие файлы были затронуты,
   - какие типовые проблемы исправлены (например: «исправлены типы контекста L21», «убраны лишние any», «согласованы типы метрик удачи»).

---

## How to run checks

```bash
# Установить зависимости
npm install

# Проверка типов
npm run check

# (опционально) Тесты
npm test
```

Если каких-то скриптов нет или они работают иначе — обнови README или добавь комментарий в PR.

---

Если хочешь, я могу следом накидать тебе ещё и **короткое описание для GitHub-PR** (Summary + Testing), чтобы ты просто вставил его при создании PR-98.
