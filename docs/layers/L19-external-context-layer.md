# L19 — External Context Layer (Environmental Perception & Adaptive Response)

L19 делает LIMINAL OS чувствительной к внешним событиям: сигнал нормализуется, оценивается по важности, переводится в давление среды, смещение смыслов и фазовое воздействие, после чего пробрасывается в глубокие слои.

## Пайплайн

1. **ContextSignal** — нормализация сырого события (source/type/payload/confidence/timestamp).
2. **ContextRelevance** — оценка значимости события для целей, устойчивости и смысла.
3. **ContextPressure** — числовое давление среды + цветовая шкала (🟦 → 🟪).
4. **ContextMeaningShift** — дельта смысла и затронутая ось (`inner|outer|goal`).
5. **ContextPhaseImpact** — смещение фазового состояния (`towards_solid|towards_liquid|towards_vapor`).
6. **ContextBridge** — упаковка данных для L17 (вес по времени), L18 (дельта смысла), L14–L16 (фазовое смещение).

## API

Главная функция: `L19_ExternalContext(signal)` из `layers/L19_ExternalContext/index.ts`.

Возвращает:
- нормализованный сигнал;
- релевантность и давление среды с цветом;
- дельту смысла и фазовое влияние;
- summary-строку для логов и подсказки для внутренних слоёв.

## Пример

```ts
const bridge = L19_ExternalContext({
  source: "system",
  type: "metric",
  payload: { cpu_usage: 0.83 },
  timestamp: 1732829384,
  confidence: 0.96,
});

// bridge.summary → source=system | type=metric | reason=cpu_load_high | relevance=0.83 | pressure=0.77(🟧) | meaningDelta=-0.71@outer | phase=towards_solid
```
