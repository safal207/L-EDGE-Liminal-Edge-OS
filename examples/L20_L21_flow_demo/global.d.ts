declare module "../../layers/L21_FlowAlignment/index.js" {
  // Minimal typing to consume the JS layer in the demo without pulling full TS conversion
  export function L21_FlowAlignment(
    innerState: any,
    contextState: any,
    phaseState: any,
    collectiveState?: any
  ): any;
}
